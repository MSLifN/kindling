# hello-agent-py

A tiny sample for validating an agent-service flow.

## What to do

1. Copy `.env.sample` from the repo root to `.env`.
2. Add your Foundry project and agent identifiers.
3. Run the sample to confirm your local environment is wired correctly.
